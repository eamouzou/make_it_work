<h4><% @contestants.each do |contestant| %> </h4>
    <p><section id="projects-<%= contestant.id %>"
    <h4><%=contestant.name %></h4>
    <h4>Projects: <% contestant.projects.each do |project| %>
                    <%= project.name %>
                    <% end %></h4>
    </section></p>
<% end %>
